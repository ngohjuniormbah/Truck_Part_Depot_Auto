import { Product, Review, GalleryItem, Order } from '../types';

// Hardcoded products cleared - all products are added dynamically via the Admin portal
export const INITIAL_PRODUCTS: Product[] = [];

// Hardcoded reviews cleared - all customer reviews will be added via the Admin portal
export const INITIAL_REVIEWS: Review[] = [];

// Hardcoded gallery pictures cleared - all pictures will be added via the Admin portal
export const INITIAL_GALLERY: GalleryItem[] = [];

export const INITIAL_ORDERS: Order[] = [];
